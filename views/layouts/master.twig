<!DOCTYPE html>
<html lang="{{ store.locale }}" dir="{{ store.locale == 'ar' ? 'rtl' : 'ltr' }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ page.meta_description }}">
    <meta name="keywords" content="{{ page.meta_keywords }}">
    <meta property="og:title" content="{{ page.title }}">
    <meta property="og:description" content="{{ page.meta_description }}">
    <meta property="og:image" content="{{ store.logo }}">
    <meta property="og:url" content="{{ request.url }}">
    <title>{{ page.title }} | {{ store.name }}</title>

    {# Salla Twilight Head - جميع سكريبتات وملفات Twilight الأساسية #}
    {{ twilight.head() }}

    {# Google Fonts - خط Tajawal الاحترافي للعربية #}
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800;900&display=swap" rel="stylesheet">

    {# Font Awesome Icons - أيقونات احترافية #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-QDAkCKwIuBRESQd1i2BzHbMIVc6ktlW+V6mask5c4D798JXc8jof8kqobQyTXn0dXjP83I08QT60JFDTRcnotw==" crossorigin="anonymous" referrerpolicy="no-referrer">

    {# Theme CSS - ملف التنسيقات الرئيسي #}
    <link rel="stylesheet" href="{{ 'assets/css/app.css' | asset }}">

    {# Customizable CSS Variables - متغيرات التخصيص للعميل #}
    <style>
        :root {
            {# الألوان الأساسية للثيم - يمكن تخصيصها من لوحة الإدارة #}
            --primary-color: {{ theme.settings.primary_color | default('#C9A84C') }};
            --primary-light: {{ theme.settings.primary_light | default('#E8D48B') }};
            --primary-dark: {{ theme.settings.primary_dark | default('#8B7C3E') }};

            {# ألوان الخلفية والبطاقات #}
            --bg-color: {{ theme.settings.bg_color | default('#0A0A0F') }};
            --card-color: {{ theme.settings.card_color | default('#12121A') }};
            --card-hover: {{ theme.settings.card_hover | default('#1A1A25') }};
            --border-color: {{ theme.settings.border_color | default('#1E1E2A') }};

            {# ألوان النصوص #}
            --text-primary: #FFFFFF;
            --text-secondary: #9CA3AF;
            --text-muted: #6B7280;

            {# الخطوط والمسافات #}
            --font-family: 'Tajawal', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 20px;
            --radius-xl: 24px;
            --transition-speed: 0.3s;
        }

        {# الوضع الليلي الافتراضي للثيم #}
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            background-color: var(--bg-color);
            color: var(--text-primary);
            font-family: var(--font-family);
            line-height: 1.6;
            scroll-behavior: smooth;
        }
    </style>
</head>
<body class="velora-body">
    {# تخطي إلى المحتوى الرئيسي - مهم للوصولية #}
    <a href="#main-content" class="skip-link">{{ trans('accessibility.skip_to_content', 'اذهب إلى المحتوى الرئيسي') }}</a>

    {# مكون الرأس - يحتوي على القائمة العلوية والشعار والقائمة الرئيسية #}
    {% include 'components/header.twig' %}

    {# قائمة الهاتف المحمول - تظهر عند الضغط على الهامبرجر #}
    {% include 'partials/mobile-menu.twig' %}

    {# نافذة البحث - تظهر عند الضغط على أيقونة البحث #}
    {% include 'partials/search-overlay.twig' %}

    {# المحتوى الرئيسي - يتغير حسب الصفحة #}
    <main id="main-content" role="main">
        {% block content %}{% endblock %}
    </main>

    {# مكون التذييل - يحتوي على الروابط والاشتراك والتواصل #}
    {% include 'components/footer.twig' %}

    {# زر العودة إلى الأعلى - يظهر عند التمرير للأسفل #}
    <button id="back-to-top" class="back-to-top" aria-label="{{ trans('accessibility.back_to_top', 'العودة إلى الأعلى') }}">
        <i class="fas fa-chevron-up"></i>
    </button>

    {# سكريبتات Twilight الضرورية - يجب أن تكون في النهاية #}
    {{ twilight.footer() }}

    {# JavaScript الثيم - يحتوي على التفاعلات والميزات #}
    <script src="{{ 'assets/js/app.js' | asset }}"></script>

    {# نص مخفي للمتغيرات العامة المستخدمة في JS #}
    <script>
        window.themeConfig = {
            locale: '{{ store.locale }}',
            isRTL: {{ store.locale == 'ar' ? 'true' : 'false' }},
            storeName: '{{ store.name }}',
            cartCount: {{ cart.count | default(0) }}
        };
    </script>
</body>
</html>
